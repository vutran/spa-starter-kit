web:
    image: node
    ports:
        - "4000:4000"
        - "4001:4001"
    volumes:
        - .:/var/www/html
    working_dir: "/var/www/html"
    environment:
        NODE_ENV: development
        PORT: 4000
    command: bash -c "npm install && npm start"
